server {
    listen 3000;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        
        # PENTING: Agar React Router jalan saat refresh
        try_files $uri $uri/ /index.html;
    }
}